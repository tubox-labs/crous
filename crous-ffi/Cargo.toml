[package]
name = "crous-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "C FFI bindings for Crous encoder/decoder"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
crous-core = { workspace = true }
serde_json = { workspace = true }
